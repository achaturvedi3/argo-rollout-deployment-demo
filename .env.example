# Sample Environment Variables for GitHub Secrets
# DO NOT commit this file with actual values!
# Copy this template and fill in your actual values in GitHub Secrets

# Docker Hub Configuration
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PASSWORD=your-dockerhub-password-or-token

# AWS Configuration
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
EKS_CLUSTER_NAME=argo-demo-cluster

# ArgoCD Configuration
ARGOCD_SERVER=argocd.example.com
ARGOCD_AUTH_TOKEN=your-argocd-auth-token

# Optional: ECR Configuration (if using ECR instead of Docker Hub)
# ECR_REGISTRY=123456789012.dkr.ecr.us-east-1.amazonaws.com
# ECR_REPOSITORY=nginx-rollout-demo
